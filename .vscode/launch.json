{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: build",
      "request": "launch",
      "command": "yarn run build",
      "cwd": "${workspaceFolder}"
    }
  ]
}